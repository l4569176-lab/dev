# 工作学习记录

## ethercat应用程序

`ethercatctl`将卸载原来的驱动，变成`ec_XXX`驱动。

1、`igb` --> `ec_igb`驱动。被指定的网口会变成`EtherCAT0`接口。

然后可以用`ethercat`指令查看状态

refer: [ethercat应用程序使用教程](https://www.cnblogs.com/wsg1100/p/14433632.html)

## x86 grub环境变量

/dev/sda2和/dev/sda3分别是环境变量和冗余环境变量文件系统。其目录下的grubenv存储了环境变量

```shell

$ cat grubenv
# GRUB Environment Block
default=rootfs_erofs2
timeout=3
######################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################################*

$ ls -lh
total 13K
-rw-r--r-- 1 root root 1.0K Dec 17 11:47 grubenv
drwx------ 2 root root  12K Jan  1  2022 lost+found
```

一共1024个字节，第一行为标题。第二行开始为每一个环境变量。最后一行为空余空间。最后一个字节为序列号。当两个环境变量都有效的情况下。最后一个字节数字大的表示当前使用的环境变量。

## 内核调试方式

### 1. 常用调试方法

- 打印日志调试使用`printk()`在内核中输出调试信息，查看`dmesg`日志。可设置不同的日志级别（如`KERN_INFO`、`KERN_ERR`等）。
- 动态调试（`Dynamic Debug`）通过`dynamic_debug`机制，在不重启内核的情况下动态控制特定代码的日志输出。使用`/sys/kernel/debug/dynamic_debug/control`文件配置。

### 2. 模块参数调试驱动编写时加入模块参数，通过加载模块时设置参数，辅助调试和功能切换。

### 3. 内核调试工具

- `GDB`（`GNU`调试器）可通过`kgdb`或`gdb`远程调试内核，支持断点、单步、变量查看等。常配合`QEMU`、`JTAG`或串口等方式进行内核级调试。
- `KDB`（内核调试器）一个轻量级的内核内置调试器，可直接在终端中调试内核。
- `ftrace`内核自带的函数跟踪工具，可追踪函数调用、执行时间、事件等。
- `perf`用于性能分析、热点函数定位、查看系统瓶颈等。
- `strace`/`ltracestrace`跟踪系统调用，`ltrace`跟踪库调用，辅助调试用户态与内核交互。
- `kprobes`/`uprobes`动态插桩，监控或更改内核、用户空间的函数行为。
- `trace-cmd`/`KernelSharkftrace`的高级可视化工具，便于分析跟踪日志。

### 4. 辅助调试机制

- `procfs/sysfs`调试接口通过`/proc`、`/sys`下自定义接口暴露驱动信息供用户空间查看和修改。
- 内核`Oops`、`Panic`分析利用`Oops`信息定位崩溃点，可配合`addr2line`等工具转换源码行号。
- 内核`dump`（`kdump`/`crash`）发生严重错误时生成内存转储，后续分析内核状态。
- 使用虚拟机/模拟器如`QEMU`搭建测试环境，配合`gdb`便于调试和回溯。

### 5. 调试流程建议

- 初步定位：`printk`/`dmesg`日志
- 细致分析：`ftrace`、`perf`、`strace`、`gdb`等
- 动态追踪：`kprobes`、`dynamic_debug`、`trace-cmd`
- 崩溃回溯：`Oops`、`kdump`、`crash`工具
- 分析性能瓶颈：`perf`、`ftrace`、`systemtap`

`ZED_ALLOW_EMULATED_GPU=1 zed `

```
DTB_FILE_NAME=stm32mp257f-hcq0p.dtb 

BL33_CFG=/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/output/images/u-boot.dtb 

STM32MP25=1 

STM32MP_LPDDR4_TYPE=1 

STM32MP_SDMMC=1 

STM32MP_EMMC=1 

PSA_FWU_SUPPORT=1 

STM32MP_DDR_FW:=/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/board/hcfa/hcq0p/firmware/linux/ddr/lpddr4_pmu_train.bin 

TRUSTED_BOARD_BOOT=1 MBEDTLS_DIR=/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/output/build/mbedtls-v3.6.2 

DYN_DISABLE_AUTH=1 

GENERATE_COT=1 

ROT_KEY=/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/board/hcfa/hcq0p/tools/hcq0p/keys/sign_keys/privateKey00.pem 

ROT_KEY_PWD=hcfa 

DECRYPTION_SUPPORT=1 

ENCRYPT_BL31=1 

ENCRYPT_BL32=1 

ENC_KEY=11c06a45bbf018a8ce1b99a4b3f62512b612f1c296fb647f3182ed1d6b821874 

ENC_NONCE=ae5dce94d71f69b4563f1b1e 

PLAT=stm32mp2 

ARM_ARCH_MAJOR=8 

ARCH=aarch64 

BL32=/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/output/images/tee-header_v2.bin BL32_EXTRA1=/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/output/images/tee-pager_v2.bin 

BL32_EXTRA2=/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/output/images/tee-pageable_v2.bin 

SPD=opteed 

BL33=/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/output/images/u-boot-nodtb.bin 

CPPFLAGS="-I/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/output/host/include" 

LDLIBS="-L/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/output/host/lib -Wl,-rpath,/home/lll/Desktop/project/hcq0p/rootfs/hcq0p_rootfs/output/host/lib -lcrypto" ; 
```